Basic C# Dictionary and Array Sorting Program

1. Loads internal wordlist into dictionary 
2. Provides options to insert, find, delete word and print the dictionary. 
3. Converts dictionary to Array. 
4. Provides the same options but for the Array along with the ablity to sort the array using bubble sort or merge sort.

